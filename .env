# Environment configuration for dsk-electron-react-project-manager
# Rename this file to .env and adjust values as needed.

# Populate the local SQLite database with seed data (users, projects, tasks)
# Valid values: true | false (case insensitive)
SEED_DEV_DATA=true
