#!/bin/sh

ROOT_DIR="$(git rev-parse --show-toplevel 2>/dev/null)"

if [ -z "$ROOT_DIR" ]; then
  echo "[commit-msg] Unable to determine repository root." >&2
  exit 1
fi

node "$ROOT_DIR/scripts/git/validate-commit-msg.mjs" "$@"
